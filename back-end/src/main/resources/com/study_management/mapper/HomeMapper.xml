<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study_management.mapper.HomeMapper">

	<insert id = "register" parameterType = "userDTO.UserDTO">
		INSERT INTO user (
			userID, userName, gender, userTel, userEmail,
			userNum, password, grade, state, dept)
		VALUES(
				  #{userID}, #{userName}, #{gender}, #{userTel}, #{userEmail},
				  #{userNum}, #{password}, #{grade}, #{state}, #{dept} );
	</insert>

	<select id = "login" resultType = "UserDTO">
		SELECT userNum, password
		FROM user
		WHERE userNum = #{userNum} AND password = #{password}
	</select>

	<update id = "modifyPassword">
		UPDATE user
		SET   password = #{password}
		WHERE userID = #{userID}
	</update>

</mapper>
